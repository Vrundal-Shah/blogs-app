// strings that are fetched from the database and displayed to the user
export function standardizeDisplayString(s: string): string {
  // the string is converted to lowercase
  // and all the spaces are removed

  return s.toLowerCase();
}
